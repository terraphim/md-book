# Rust
/target/
**/*.rs.bk
Cargo.lock

# Output directories
/dist/
/build/
/out/
/test_out

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Environment variables (contain actual secrets)
.env
.env.local
.env.production
.env.staging

# Note: .env.1password is NOT ignored because it contains only secret references
# (op://vault/item/field format), not actual secrets, and should be committed

# Logs
*.log
logs/

# Node modules (for frontend tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Temporary files
tmp/
temp/
# Build scratch directories, not test inputs. The bare `test_*` this replaces
# also matched tests/assets/test_book_1, so that fixture was never committed
# and the structure suite passed only on machines that happened to have it.
/test_*/
!/test_book_mdbook/
!tests/assets/**

# Test output
test-results/
benchmark-results/
coverage/

# Wrangler
.wrangler/

# Build artifacts
*.wasm
*.js.map

# Backup files
*.backup
*.bak

# Local development
.cache/
.parcel-cache/