<header class="site-header">
    <div class="header-content">
        <div class="header-left">
            <button class="mobile-menu-toggle" aria-label="Toggle navigation">
                <wa-icon name="list"></wa-icon>
            </button>
            <a href="{{ path_to_root | safe }}index.html" class="header-logo" aria-label="{{ config.book.title }} home">
                <img src="{{ logo_url | safe }}" alt="" class="header-logo-img" height="48px">
            </a>
            <span class="header-title">{{ config.book.title }}</span>
        </div>
        <div class="header-right">
            {% if search_enabled %}
            <button class="mobile-search-toggle" aria-label="Search">
                <wa-icon name="magnifying-glass"></wa-icon>
            </button>
            <wa-input placeholder="Search docs..." size="small" clearable class="header-search" id="header-search-input" aria-label="Search documentation">
                <wa-icon name="magnifying-glass" slot="start"></wa-icon>
            </wa-input>
            {% endif %}

            {# Theme picker. <details> gives keyboard support and an open/close
               state without script; theme-switch.js handles the selection. #}
            <details class="theme-menu">
                <summary class="header-link" aria-label="Change theme" title="Change theme">
                    <wa-icon name="palette"></wa-icon>
                </summary>
                <ul class="theme-list" role="list">
                    {% for theme in ["light", "rust", "coal", "navy", "ayu"] %}
                    <li>
                        <button type="button" data-theme-set="{{ theme }}">{{ theme | capitalize }}</button>
                    </li>
                    {% endfor %}
                </ul>
            </details>

            {% if repo_url %}
            <div class="header-links">
                <a href="{{ repo_url | safe }}" class="header-link" target="_blank" rel="noopener" aria-label="View source repository">
                    <wa-icon name="brands/github"></wa-icon>
                </a>
            </div>
            {% endif %}
            {% if edit_url %}
            <div class="header-links">
                <a href="{{ edit_url | safe }}" class="header-link" target="_blank" rel="noopener" aria-label="Edit this page">
                    <wa-icon name="pen-to-square"></wa-icon>
                </a>
            </div>
            {% endif %}
        </div>
    </div>
</header>
